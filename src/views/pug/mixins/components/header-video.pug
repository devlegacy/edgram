mixin headerVideo(url, title, text)
  article.VideoHeader.u-firstContent
    .VideoHeader-text
      h2= title
      h3= text
    video.VideoHeader-video(src=require(`../media/${url}.mp4`), poster=require(`../img/maratones/${url}.jpg`), muted, loop)

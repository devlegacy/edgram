mixin socialMedia(sm)
  aside.SocialMedia
    ul.SocialMedia-list
      each item in sm
        li.SocialMedia-item
          a.SocialMedia-link(href=`${item.url}`, target="_blank")
            i(class=`fa fa-${item.name}`)
